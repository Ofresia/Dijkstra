import java.util.Map;

/**
 * Provides an implementation of Dijkstra's algorithm to find the shortest paths from a source node.
 */
public class Dijkstra {

    /**
     * Computes the shortest paths from the given start node to all other nodes using Dijkstra's algorithm.
     * 
     * @param graph The graph in which to find shortest paths.
     * @param startNode The node from which to start the shortest path computation.
     * @return A map where each key is a node and the value is the shortest distance from the start node to that node.
     */
    public static Map<String, Integer> findShortestPaths(Graph graph, String start) {
        // Implementation here
	}
}